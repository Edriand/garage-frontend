version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  cache:
    paths:
      - .npm/**/*
      - .next/cache/**/*
      - node_modules/**/*
